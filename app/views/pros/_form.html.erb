<%= form_for([@dilema, @dilema.pros.build]) do |f| %>
  <div class="form-inline">
    <% f.label :name %>
    <%= f.text_field :name, class: "span2" %>
	<% f.label :value %>
    <%= f.select :value, [1, 2, 3, 4, 5], class: "span1" %>
    <%= f.submit "Add Pro", class: 'btn' %>
  </div>
<% end %>